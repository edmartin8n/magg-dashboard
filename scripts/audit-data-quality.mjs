import fs from 'node:fs'; import path from 'node:path'; import { loadDb } from '../src/data/store.mjs'; import { getDataCompleteness } from '../src/services/selectors.mjs'; const db=loadDb(); const rows=db.deals.map(d=>({reference:d.reference,name:d.name,migration_status:d.migration_status,completeness:getDataCompleteness(d),issues:d.data_quality_issues})); const out=path.resolve('private/reports/data-quality-report.json'); fs.mkdirSync(path.dirname(out),{recursive:true}); fs.writeFileSync(out,JSON.stringify(rows,null,2)); console.log(JSON.stringify({status:'ok',deals:rows.length,needs_review:rows.filter(r=>r.migration_status==='needs_review').length,report:out},null,2));
