import fs from 'node:fs'; import path from 'node:path'; import { migrateLegacyData } from '../src/domain/migration.mjs'; import { CrmDatabaseSchema } from '../src/domain/schema.mjs';
const inputDir=path.resolve('private/migration/legacy-input-20260715'), outputPath=path.resolve('private/data/crm.json'), reportPath=path.resolve('private/reports/migration-report.json'); const legacyDeals=JSON.parse(fs.readFileSync(path.join(inputDir,'deals.json'),'utf8')), legacyBrokers=JSON.parse(fs.readFileSync(path.join(inputDir,'brokers.json'),'utf8')), manifest=JSON.parse(fs.readFileSync(path.join(inputDir,'manifest.json'),'utf8')); const db=migrateLegacyData({legacyDeals,legacyBrokers,manifest}); CrmDatabaseSchema.parse(db); fs.mkdirSync(path.dirname(outputPath),{recursive:true}); fs.mkdirSync(path.dirname(reportPath),{recursive:true}); fs.writeFileSync(outputPath,JSON.stringify(db,null,2)); fs.writeFileSync(reportPath,JSON.stringify(db.migration_report,null,2)); console.log(JSON.stringify({status:'ok',input_deals:db.migration_report.input_deals,output_deals:db.migration_report.output_deals,input_brokers:db.migration_report.input_brokers,contacts:db.migration_report.output_contacts,relationships:db.migration_report.relationships_migrated,documents:db.migration_report.documents_migrated,review_records:db.migration_report.ambiguous_records.length},null,2));
